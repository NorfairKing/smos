### Changed

* Fixed that `smos-archive` parsed the directory as an option instead of an argument.
* Fixed that `smos-github import` parsed the url as an option instead of an argument.
* Renamed `smos-sync-client` to `smos-sync`.
