name: smos-cli
version: 0.0.0
category: Smos
author: Tom Sydney Kerckhove
maintainer: syd@cs-syd.eu
license: MIT

dependencies:
  base

library:
  source-dirs: src/
  ghc-options: -Wall
  dependencies:
  - bytestring
  - monad-logger
  - password
  - text
  - validity

tests:
  smos-cli-test:
    main: Spec.hs
    source-dirs: test
    build-tools: sydtest-discover
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    - -Wall
    dependencies:
    - genvalidity-sydtest
    - smos-cli
    - sydtest

